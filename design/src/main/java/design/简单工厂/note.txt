
简单工厂


    工厂类角色：
            这是本模式的核心，含有一定的商业逻辑和判断逻辑，根据逻辑不同，产生具体的工厂产品, OperationFactory

    抽象产品角色：
            它一般是具体产品继承的父类或者实现的接口。由接口或者抽象类来实现,Operation

    具体产品角色：
            工厂类所创建的对象就是此角色的实例。在java中由一个具体类实现OperationAdd

案例：　计算器.


缺点：新增一个运算需要修改OperationFactory类, 违背开闭原则的。

可以参考 工厂方法的优化.